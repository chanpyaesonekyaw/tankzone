PORT=3000
HOST=0.0.0.0
CLIENT_ORIGIN=https://tankzone.vercel.app,http://localhost:5174,http://localhost:5173,http://localhost:3001

JWT_SECRET=dev_change_me
JWT_EXPIRES_IN=7d

# MongoDB (optional, enables email/password accounts + score history)
MONGO_URI=

# Server tick (authoritative simulation) and broadcast rates.
TICK_RATE=30
STATE_BROADCAST_RATE=15
LEADERBOARD_RATE=2


