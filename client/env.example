# Client build-time config (Vite).
# These values are PUBLIC (embedded into the JS bundle). Do NOT put secrets here.
#
# Dev: leave these unset and rely on Vite proxy in `vite.config.js`.
# Prod (different domains): set these as Environment Variables in the Vercel *client* project.

# Base URL for REST calls (e.g. https://tankzone-server.vercel.app)
VITE_BACKEND_URL=

# Optional: separate base URL for Socket.IO (defaults to VITE_BACKEND_URL if empty)
VITE_SOCKET_URL=


